en-US:
  helpers:
    select:
      prompt: Please select
    submit:
      create: Create %{model}
      submit: Save %{model}
      update: Update %{model}
  support:
    array:
      last_word_connector: ", and "
      two_words_connector: " and "
      words_connector: ", "
